/*const numbers = [10, 20, 30, 40, 50];

for (let i = 0; i < numbers.length; i++) {
    console.log((i + 1) + '. ' + numbers[i] + ' * 2 = ' + (numbers[i] * 2));
} */

const numbers = [10, 20, 30, 40, 50];

let i = 0;
while (i < numbers.length) {
    console.log((i + 1) + '. '+ numbers[i] +' * 2 = ' + (numbers[i] * 2));
    i++;
}